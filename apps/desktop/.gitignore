# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# The repository root's .gitignore has a bare `package.json` line, which
# swallows this app's manifest and leaves a fresh clone unable to install or
# build. Re-include it here — a nested .gitignore wins over the parent.
!package.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
